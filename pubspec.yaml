name: ib_points_calc
description: IB score calculator for normal subjects

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  animator: ^1.0.0+1
  cupertino_icons: ^0.1.2
  material_design_icons_flutter: ^3.2.3895
  shared_preferences: ^0.5.4+3
  autocomplete_textfield: ^1.6.4

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true